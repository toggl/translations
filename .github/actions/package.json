{
  "name": "translations",
  "private": true,
  "engines": {
    "node": ">=16.20.0",
    "yarn": ">=1.22.4"
  },
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "workspaces": [
    "packages/*"
  ]
}
